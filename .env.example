# Copy to .env. Nothing here is required for the demo dataset to run.

# SQLite for local dev. See README "Switching to PostgreSQL" to move over.
DATABASE_URL="file:./dev.db"

# ---------------------------------------------------------------------------
# AI daily brief
# ---------------------------------------------------------------------------
# "mock" (default) uses a rule-based composer and needs no API key.
# "openai" or "claude" call the real APIs and fall back to mock on failure.
AI_PROVIDER="mock"

OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"

ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-sonnet-5"

# ---------------------------------------------------------------------------
# Scheduled jobs
# ---------------------------------------------------------------------------
# Required in production: POST /api/jobs/* checks this as a bearer token.
# With no secret set, the job routes are allowed only outside production.
CRON_SECRET=""

# Used by `npm run cron` (the long-running local scheduler).
CRON_SCHEDULE="0 20 * * 1-5"
CRON_TIMEZONE="Asia/Taipei"
